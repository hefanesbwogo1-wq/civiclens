[project]
name = "civiclens"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "python-dotenv",
  "supabase",
  "pydantic"
]

[tool.vercel]
entrypoint = "api/index.py"
